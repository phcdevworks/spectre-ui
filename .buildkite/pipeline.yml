steps:
  - label: ':node: spectre-ui verify'
    key: 'verify'
    command:
      - 'node --version'
      - 'npm --version'
      - 'npm ci'
      - 'npm run ci:verify'
    agents:
      queue: 'default'
      node_version: '22'
    artifact_paths:
      - 'dist/**/*'
    timeout_in_minutes: 20
